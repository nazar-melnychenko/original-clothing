import { AddCurrencySignPipe } from './add-currency-sign.pipe';

describe('AddCurrencySignPipe', () => {
  it('create an instance', () => {
    const pipe = new AddCurrencySignPipe();
    expect(pipe).toBeTruthy();
  });
});
